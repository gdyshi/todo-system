[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test*
addopts =
    -v
    --cov=app
    --cov-report=xml
    --cov-report=term
asyncio_mode = auto
env =
    PYTEST_CURRENT_TEST=1
